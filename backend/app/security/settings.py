"""
4. settings.py

Purpose:

Environment-related settings, e.g., .env reading, database URLs, JWT expiry defaults.

Action:

Keep it if you want central config.

Could merge with config.py if it’s very small.
"""